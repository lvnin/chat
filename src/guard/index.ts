/*
 * author: ninlyu.dev@outlook.com
 */
//import { JwtAuthGuard } from '@guard/jwt.guard';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const globalGuards = (app: any) => {
  // app.useGlobalGuards(JwtAuthGuard);
};
